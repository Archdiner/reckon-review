{
  "name": "reckon-record-study",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "How much of a change survives in the record: measuring mechanism-answerability of merged PR records, and whether that record is reproducible from the diff alone.",
  "scripts": {
    "study": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@reckon/core": "file:../vendor/reckon-core-0.5.0.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
