node_modules/
dist/
.env
.env.*
!.env.example
*.pem
*.log
.DS_Store

# Delivery bundle, assembled from example/ and out/ for sending. Both sources are already
# tracked or regenerable, so versioning a third copy would just be drift waiting to happen.
session-results/
